name: Uniforma articoli e indici (disattivato)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow temporaneo disattivato. Da rimuovere con Codex."
